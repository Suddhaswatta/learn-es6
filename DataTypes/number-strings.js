const n = 10
const circle = (r)=>{
    return Math.PI*Math.pow(r,2)
}
console.log(`Area of circle ${circle(3)} `);
console.log(typeof n);

console.log(Math.PI);
console.log(Math.random());

/**
 * Strings
 */

